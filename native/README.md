# native/

C++ mosaic engine **and** local host (HTTP API, jobs, CLI). See
[`../docs/native.md`](../docs/native.md).

```bash
bash native/build.sh    # builds lego_server, lego_cli, and runs tests
```

Requires a C++17 compiler, CMake, and SQLite 3. No JDK.
